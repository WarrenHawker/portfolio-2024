const Footer = () => {
  return (
    <footer>
      <p>This site was built using React, React-Router and Frame Motion</p>
      <p>Copywrite {new Date().getFullYear()} By Warren Hawker</p>
    </footer>
  );
};

export default Footer;
